import { KuvaLogEntry } from "./KuvaLogEntry.mjs";
import { Locale } from "warframe-worldstate-data";
import { SortieData } from "warframe-worldstate-data/types";

//#region lib/supporting/Dependency.d.ts
/**
 * Dependency Object
 */
interface Dependency {
  /**
   * Kuva data for parsing
   */
  kuvaData?: KuvaLogEntry[];
  /**
   * Locale to use for translations
   */
  locale: Locale;
  /**
   * Generic logger to use if needed
   */
  logger?: {
    debug: (message: string) => void;
  };
  /**
   * Sentint Data for parsing
   */
  sentientData?: {
    start: number;
    end: number;
  };
  /**
   * Sortie data for parsing
   */
  sortieData?: SortieData;
  /**
   * Character name for traders
   */
  character?: string;
}
//#endregion
export { Dependency };